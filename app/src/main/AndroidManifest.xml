<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pms.com.patientmonitor" >

    <application
        android:name="com.orm.SugarApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >
        <activity android:name=".LoginActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="DATABASE"
            android:value="pms.db" />
        <meta-data
            android:name="VERSION"
            android:value="1" />
        <meta-data
            android:name="QUERY_LOG"
            android:value="true" />
        <meta-data
            android:name="DOMAIN_PACKAGE_NAME"
            android:value="Entity" />

        <activity android:name=".DoctorHomeActivity" >
        </activity>
        <activity android:name=".PatientHomeActivity" >
        </activity>
        <activity
            android:name=".MyPatientsListActivity"
            android:theme="@style/AppTheme" >
        </activity>
        <activity android:name=".AddReportActivity" >
        </activity>
        <activity android:name=".ReportListActivity" >
        </activity>
        <activity android:name=".ViewReportActivity" >
        </activity>
        <activity android:name=".PatientBioActivity" >
        </activity>
    </application>

</manifest>
